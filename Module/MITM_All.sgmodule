#!name=MitM All
#!desc=对所有端口为 433 的主机名执行 MitM，除了 Apple 和其他不能被解密的站点，您仍然需要配置 CA 证书并启用 MitM 开关。

[MITM]
hostname = -*.apple.com, -*.icloud.com, -*.mzstatic.com, -*.crashlytics.com, -*.facebook.com, -*.instagram.com, *
