#!name=MitM All
#!desc=临时对所有网站开启 MitM，除了 Apple 和其他不能被解密的站点，您仍然需要配置 CA 证书并启用 MitM 开关。

[MITM]
skip-server-cert-verify = true
tcp-connection = true
hostname = -*.apple.com, -*.icloud.com, -*.mzstatic.com, -*.crashlytics.com, -*.facebook.com, -*.instagram.com, *
